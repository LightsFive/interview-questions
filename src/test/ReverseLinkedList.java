/*
package test;

public class ReverseLinkedList {

    static void reverse(Node head) {

        Node node = head;
        Node curr = head.next;
        while (node != null && node.next != null) {

            curr = node.next; -- B
            Node next = temp.next; -- C

            temp.next = node; -- B -> A

            node = next;
        }


    }
}
*/
